num=int (input("Enter a Number:"))
def factorial_recursion(x):
if x==0 or x==1:
return 1
else:
return x * factorial_recursion(x - 1)
fact_num=factorial_recursion(num)

3

print("Factorial of ",num," is",fact_num)
